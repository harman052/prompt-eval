# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Tooling caches
.ruff_cache/
.mypy_cache/
.pytest_cache/
.coverage
htmlcov/

# Secrets
.env

# Run artifacts, regenerated by the CLI. `data/`, `output/` and `baseline/`
# stay tracked so a fresh clone can run `evaluate` and `compare` immediately.
eval_results/
comparison_results/
